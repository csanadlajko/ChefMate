<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ChefMate_YR6LYT.MainPage"
             xmlns:local="clr-namespace:ChefMate_YR6LYT"
             x:DataType="local:MainPageViewModel"
             Loaded="OnMainPageLoaded"
             Title="RecipePage">

    <Grid RowDefinitions="Auto, Auto, *" RowSpacing="10" WidthRequest="300" Padding="0, 10">
        <HorizontalStackLayout Spacing="10" HorizontalOptions="Center">
            <Button Text="Add Recipe" Command="{Binding ShowAddRecipePageCommand}" BackgroundColor="LightSeaGreen"/>
            <Button Text="Edit Selected Recipe" Command="{Binding ShowAddRecipePageCommand}" BackgroundColor="Orange" />
            <Button Text="Delete Selected Recipe" Command="{Binding DeleteRecipeCommand}" BackgroundColor="OrangeRed"/>
        </HorizontalStackLayout>
        <Border StrokeThickness="5" Stroke="LightSkyBlue" Grid.Row="2" StrokeShape="RoundRectangle 10">
            <CollectionView ItemsSource="{Binding Recipes}"
                            SelectedItem="{Binding SelectedRecipe}"
                            SelectionMode="Single"
                            Background="WhiteSmoke">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="local:Recipes">
                    <!--<SwipeView></SwipeView> swipe to share, might be implemented later on -->
                    <Border StrokeThickness="2" Stroke="Black" Margin="5" Padding="5" StrokeShape="RoundRectangle 10">
                        <Grid ColumnDefinitions="3*,*">
                            <VerticalStackLayout>
                                <Label Text="{Binding Name}" FontSize="Medium" FontAttributes="Bold"/>
                                <Label Text="{Binding CreatedAt, StringFormat='Creation time: {0}'}" FontSize="Small"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>
    </Grid>
    

</ContentPage>
